# AMD/Nvidia GPU support and a generic DRM fallback - all disabled in
# the default kernel config (Intel-focused BSP defaults).
CONFIG_DRM_AMDGPU=m
CONFIG_DRM_RADEON=m
CONFIG_DRM_NOUVEAU=m
CONFIG_DRM_SIMPLEDRM=y
